$(document).ready(function() {

    // $('.cell').submit(function(event) {
    //     event.preventDefault();
    //     $.post( "/makeMove", $( ".cell" ).serialize(), function( data ) {
    //         data.split("").forEach(function(number,i) {
    //             $($('td input')[i]).val(number);
    //         });
    //     });
    // });

});
